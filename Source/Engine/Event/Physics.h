#pragma once
namespace shovel
{

	/*
	inline b2Vec2 to_b2(const vec2 v) { return b2Vec2{v.x v.y}; }
	inline vec2 to_vec2(const b2vec2 v) { return vec2{v.x v.y}; }

	class Physics
	{
	public:
		Physics() = default;

		bool Initalize();
		void Shutdown();

		void Update(float dt);

		vec2 WorldToPixel(const vec2)
	};
	*/
}